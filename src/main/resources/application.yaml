spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wetty_plants?useSSL=false
    username: root
    password: damr.1992
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
    show-sql: true